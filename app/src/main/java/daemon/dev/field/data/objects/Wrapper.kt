package daemon.dev.field.data.objects

class Wrapper(
                val type : Int,
                val mid : Int,
                val cur : Int,
                val max : Int,
                val bytes : ByteArray

               )